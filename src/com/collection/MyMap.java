package com.collection;

/*
 * Rehashing is the process of re-calculating the hash-code of already stored entries (Key-Value pairs), 
 * to move them to another bigger size hashmap when the threshold is reached/crossed.
 * 
 * A hash code collision in a HashMap, is a situation where two or more key objects produce the same final hash 
 * value and hence point to the same bucket location or array index.
 */
public class MyMap {

}
